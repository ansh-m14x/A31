name: A31 Kernel Build - Ultimate Bootable
on:
  push:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: ubuntu:20.04
      options: --privileged

    steps:
    - name: Checkout Source
      uses: actions/checkout@v4

    - name: Install Build Packages
      run: |
        export DEBIAN_FRONTEND=noninteractive
        apt-get update
        apt-get install -y \
        python2 python-is-python2 git ccache automake flex lzop bison gperf \
        build-essential zip curl zlib1g-dev g++-multilib libxml2-utils bzip2 \
        libbz2-dev libghc-bzlib-dev squashfs-tools pngcrush fontconfig \
        liblz4-tool make optipng maven libssl-dev pwgen bc libc6-dev-i386 \
        libx11-dev lib32z-dev libgl1-mesa-dev xsltproc unzip \
        device-tree-compiler libncurses5 libtinfo5 wget

    - name: Download Toolchains
      run: |
        mkdir -p tools
        git clone https://github.com/KAGA-KOKO/clang-6.0.2 -b clang --depth=1 tools/clang
        git clone https://android.googlesource.com/platform/prebuilts/gcc/linux-x86/aarch64/aarch64-linux-android-4.9 -b ndk-release-r16 --depth=1 tools/gcc64
        git clone https://android.googlesource.com/platform/prebuilts/gcc/linux-x86/arm/arm-linux-androideabi-4.9 -b ndk-release-r16 --depth=1 tools/gcc32

    - name: SELinux & Watchdog Patches (Safe Method)
      run: |
        # Camera & System Access (Using the code-level patch from your bootable build)
        find kernel-4.9/security/selinux/ -name "*.c" -exec sed -i 's/int selinux_enforcing = 1;/int selinux_enforcing = 0;/g' {} +
        find kernel-4.9/security/selinux/ -name "*.c" -exec sed -i 's/selinux_enforcing = 1/selinux_enforcing = 0/g' {} +
        find kernel-4.9/security/selinux/ -name "*.c" -exec sed -i 's/return selinux_enforcing;/return 0;/g' {} +

        # Watchdog Fix
        cd kernel-4.9/drivers/watchdog/mediatek/wdt/common/
        if [ -d "wdt_v2" ] && [ ! -d "wdt_v1" ]; then ln -s wdt_v2 wdt_v1; fi
        cd ../../../../../..

    - name: Connectivity & Multimedia Integration
      run: |
        # Connectivity swap (Keeping it since it was in your bootable build)
        rm -rf kernel-4.9/drivers/misc/mediatek/connectivity
        git clone https://github.com/KAGA-KOKO/kernel-oppo6765 -b base-oss --depth=1 temp_kaga
        mv temp_kaga/drivers/misc/mediatek/connectivity kernel-4.9/drivers/misc/mediatek/
        rm -rf temp_kaga
        
        # Header path fix
        find kernel-4.9 -path kernel-4.9/drivers/misc/mediatek/connectivity -prune -o -type f \( -name "*.c" -o -name "*.h" \) -exec sed -i 's|<soc/qcom/|<linux/soc/qcom/|g' {} +

    - name: Ultimate Config Tweaks (The Bootable Version)
      run: |
        DEFCONFIG="kernel-4.9/arch/arm64/configs/oppo6765_19581_defconfig"
        
        # 1. Multimedia & Green Screen Killers (Safe Implementation)
        echo "CONFIG_MTK_SMI_EXT=y" >> $DEFCONFIG
        echo "CONFIG_MTK_SMI_BWC=y" >> $DEFCONFIG
        echo "CONFIG_MTK_IOMMU=y" >> $DEFCONFIG
        echo "CONFIG_MTK_VIDEOCODEC_DRIVER=y" >> $DEFCONFIG
        echo "CONFIG_VIDEO_MEDIATEK_VPU=y" >> $DEFCONFIG
        echo "CONFIG_VIDEO_MEDIATEK_VCODEC=y" >> $DEFCONFIG
        
        # 2. Camera Framework
        echo "CONFIG_VIDEO_V4L2=y" >> $DEFCONFIG
        echo "CONFIG_MTK_V4L2_ADAPTER=y" >> $DEFCONFIG
        echo "CONFIG_MTK_IMGSENSOR=y" >> $DEFCONFIG
        echo "CONFIG_MTK_CAM_CAL=y" >> $DEFCONFIG
        
        # 3. Flashlight Fix
        sed -i 's/CONFIG_MTK_FLASHLIGHT_MT6370=n/CONFIG_MTK_FLASHLIGHT_MT6370=y/g' $DEFCONFIG
        echo "CONFIG_RT_FLASHLIGHT=y" >> $DEFCONFIG
        
        # 4. SELinux: Keeping it 'y' to prevent panic, but forcing permissive
        sed -i 's/CONFIG_SECURITY_SELINUX=n/CONFIG_SECURITY_SELINUX=y/g' $DEFCONFIG
        echo "CONFIG_SECURITY_SELINUX_BOOTPARAM=y" >> $DEFCONFIG
        echo "CONFIG_SECURITY_SELINUX_BOOTPARAM_VALUE=0" >> $DEFCONFIG
        
        # 5. CMDLINE (Permissive but NO 'selinux=0' to avoid early crash)
        sed -i '/CONFIG_CMDLINE=/d' $DEFCONFIG
        echo 'CONFIG_CMDLINE="console=tty0 console=ttyMT3,921600n1 androidboot.selinux=permissive buildvariant=userdebug"' >> $DEFCONFIG
        echo "CONFIG_CMDLINE_FORCE=y" >> $DEFCONFIG

    - name: Compile Kernel
      run: |
        export PATH=$(pwd)/tools/clang/bin:$(pwd)/tools/gcc64/bin:$(pwd)/tools/gcc32/bin:$PATH
        export ARCH=arm64
        export SUBARCH=arm64
        export CLANG_TRIPLE=aarch64-linux-gnu-
        export CROSS_COMPILE=aarch64-linux-android-
        export CROSS_COMPILE_ARM32=arm-linux-androideabi-
        
        cd kernel-4.9
        make O=out mrproper
        make O=out oppo6765_19581_defconfig
        make -j$(nproc) O=out CC=clang CLANG_TRIPLE=$CLANG_TRIPLE CROSS_COMPILE=$CROSS_COMPILE CROSS_COMPILE_ARM32=$CROSS_COMPILE_ARM32 Image.gz-dtb

    - name: Upload Build
      uses: actions/upload-artifact@v4
      with:
        name: A31-Ultimate-Bootable-Fixed
        path: kernel-4.9/out/arch/arm64/boot/Image.gz-dtb
